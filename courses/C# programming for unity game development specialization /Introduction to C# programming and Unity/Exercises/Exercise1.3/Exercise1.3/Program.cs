using System;

namespace Exercise1
{
    //implementation of declaration of variables in VisualStudio/Monodevelop
    class MainClass
    {
        public static void Main(string[] args)
        {
            //The difference between Write and WriteLine is that
            //the former displays the just the output and the latter displays the output along with a new line character

            int age = 19;
            Console.Write(age);
            Console.WriteLine(age);
            Console.Write(age);
        }
    }
}
